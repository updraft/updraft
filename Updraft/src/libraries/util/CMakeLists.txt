cmake_minimum_required(VERSION 2.8)

LIBRARY_BUILD(util)

FIND_PACKAGE(OpenSceneGraph REQUIRED)

INCLUDE_DIRECTORIES(SYSTEM ${OPENSCENEGRAPH_INCLUDE_DIRS})
TARGET_LINK_LIBRARIES(util GeographicLib ${OPENSCENEGRAPH_LIBRARIES})
